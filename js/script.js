document.addEventListener("DOMContentLoaded", () => {
  console.log("Profile website loaded!");

  // contoh interaktif kecil
  const name = prompt("Siapa nama kamu?");
  if (name) {
    alert("Halo " + name + "! Selamat datang di profile saya 🚀");
  }
});
